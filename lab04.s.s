
.data

array: .word 1, 0, 1, 12, 0, 1, 4

.text

    la a0, array
    li a1, 7    # unsigned
    li a2, 1
prog:
#-----------------------------
# Write your code here!
 li s0, 0         # ??????p???s? t?? s0 µe 0 (?aµ?a d?e????s? a??µ?)
    li t0, 0         # ?et??t?? ??a ta st???e?a (?e????e? ap? 0)

loop:
    beq t0, a1, done # ??e???? a? te?e??se ? p??a?a?
    lw t1, 0(a0)     # F??t?s? t?? t?????t?? st???e??? t?? p??a?a
    beq t1, a2, match # ?? t? st???e?? ta?????e? µe t?? a2, p??a????µe st? match
    addi a0, a0, 4   # ?et?ßas? st? ep?µe?? st???e?? (d?e????s? 4 bytes µp??st?)
    addi t0, t0, 1   # ????s? t?? µet??t?
    j loop           # ?pa?e?????s? t?? ß?????

match:
    mv s0, a0        # ?p????e?s? t?? d?e????s?? t?? ta???ast?? st???e???
    addi a0, a0, 4   # ???????µe st? ep?µe?? st???e??
    addi t0, t0, 1   # ????s? t?? µet??t?
    j loop           # ?pa?e?????s? t?? ß?????
# Do not remove the prog label or write code above it!
#-----------------------------
done:
    addi a7, zero, 10 
    ecall
